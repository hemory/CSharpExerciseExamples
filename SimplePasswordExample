 const string password = "1234";

            Console.WriteLine("Please enter your password");
            string passwordAttemp = Console.ReadLine();

            if (passwordAttemp == password)
            {
                Console.WriteLine("Welcome to the App");
                Console.WriteLine("Please choose one of the options below:");
                Console.WriteLine("(1)Apply For Loan (2)Quit");
                string userChoice = Console.ReadLine();

                switch (userChoice)
                {
                    case "1":
                        Console.WriteLine("What is your income?");
                        double income = Convert.ToDouble(Console.ReadLine());

                        Console.WriteLine("How many years have you been with your current employer?");
                        double yearsWithCurrentEmployer = Convert.ToDouble(Console.ReadLine());

                        Console.WriteLine();
                        break;
                    case 2:
                        Console.WriteLine();
                        break;
                    default:
                        Console.WriteLine();
                        break;
                }
            }
            else
            {
                Console.WriteLine("I am sorry, that password is incomplete.");
            }

            //Make sure that num is not negative
            int num = 0;

            if (num < 0)
            {
                //assign 0 to it so that we can move on
            }
